--- a/hooks/useAPTCalculation.ts
+++ b/hooks/useAPTCalculation.ts
@@ -165,10 +165,16 @@
   let high = targetFinalHourlyRate * 2; // initial upper bound guess
   let bestGross = 0;

+  // Guard: if hours or workers are 0, or margin is 100%, rate will never scale up
+  if (inputs.workerCount <= 0 || inputs.hoursPerMonth <= 0 || inputs.marginPercent >= 100) {
+    return 0;
+  }
+
   // Expand high bound if necessary
-  while (calculateForward({ ...inputs, grossRateHourly: high }).finalHourlyRate < targetFinalHourlyRate) {
+  let expandIters = 50;
+  while (calculateForward({ ...inputs, grossRateHourly: high }).finalHourlyRate < targetFinalHourlyRate && expandIters > 0) {
     if (high === 0) high = 100;
     else high *= 2;
+    expandIters--;
   }

   // Binary search for precision
